software =
{
	version = "0.1.0";

	mx8-camera = {
		hardware-compatibility: [ "1.0" ];

		files: (
			{
				filename = "scailx-image-swupdate-mx8-camera.tar.gz";
				type = "archive";
				compressed = true;
				device = "/dev/update";
				filesystem = "ext4";
				preserve-attributes = true;
				path = "/";
			}
		);

		scripts: (
			{
				filename = "update.sh";
				type = "shellscript";
			}
		);
	};
}
